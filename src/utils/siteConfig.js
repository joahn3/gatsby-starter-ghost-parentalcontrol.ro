module.exports = {
    siteUrl: `https://eager-cray-4dfced.netlify.app, // Site domain. Do not include a trailing slash!

    postsPerPage: 12, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `ParentalControl.ro`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Controalele parentale sunt numele unui grup de setări care vă pun in control asupra conținutului pe care îl poate vedea copilul dvs.`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `Parental.ro`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#191b1f`, // Used for Offline Manifest
    themeColor: `#15171A`, // Used for Offline Manifest
}
